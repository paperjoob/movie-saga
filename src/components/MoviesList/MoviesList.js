import React, { Component } from 'react';

class MoviesList extends Component {
  // Renders the entire app on the DOM
  render() {
    return (
      <div className="movieItems">
        <p>Hello from MoviesList</p>
      </div>
    );
  }
}

export default MoviesList;